	# 变量名不能以数字开头
	# with是标识符不能作为变量名
    # k是int型格式，不能直接与字符串类型相加，要先将k转换为字符串类型才能相加
	# if else语句是以缩进来判断函数内容
	#单引号与双引号应该各自配对，进行统一
	k = -9
	if k >= 0:
    res = str(k) + "正数"
    print(res)
		else:
    print(str(k) + "负数")