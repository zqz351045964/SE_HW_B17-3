题目：
2k = -9

  if 2k>= 0:

    with = 2k +"正数"

        print(with)

    else:

    print(2K+ '负数")

修改：
变量不能以数字开头，
with是关键字不饿能作为变量名，
if和else没有配对，
引号使用错误，
K的大小写没有对应，
2K+“。。。”是int和字符串，需要用str将int类转换再操作

修改：
K = -9

if K>=0:

    gu = str(K) + "正数"
    print(gu)

else:
    
    print(str(K) + "负数")
