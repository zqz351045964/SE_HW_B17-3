#鸡兔同笼问题。假设共有鸡、兔30只，脚90只，求鸡、兔各有多少只?

S1 = 0
S2 = 0
while S1 <= 30  and  S2 <= 30:
    if S1 + S2 == 30:
        if S1 + 2 * S2 == 45:
            break
    S1 += 1
    S2 = 30 - S1
print("结果：鸡有"+ str(S1) +"只,兔有"+ str(S2) +"只\n")